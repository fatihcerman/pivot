// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Article {
  id          String   @id @default(uuid())
  title       String
  url         String   @unique
  imageUrl    String?
  category    String
  summary     String
  perspective String?  // The "PIVOT" unique take/industry impact
  sourceName  String?  // e.g., "IGN", "VGC", "Gematsu"
  isUrgent    Boolean  @default(false)
  createdAt   DateTime @default(now())
}
